# Omni CRM - Backend Environment Variables
# Copy this file to server/.env and fill in your values
# For cPanel deployment, place this as api/.env

# ============================================
# Server Configuration
# ============================================
NODE_ENV=production
PORT=5001

# ============================================
# Database Configuration
# ============================================
# Format: mysql://username:password@host:port/database_name
# Example: mysql://youruser:yourpass@localhost:3306/yourusername_omni_crm
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# ============================================
# JWT Authentication
# ============================================
# Generate a strong random secret (minimum 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long
JWT_EXPIRES_IN=7d

# ============================================
# Application URLs
# ============================================
# Frontend URL (where your React app is hosted)
CLIENT_URL=https://yourdomain.com
# Or if using subdomain: https://app.yourdomain.com

# Backend API URL (where your Node.js API is hosted)
API_URL=https://api.yourdomain.com
# Or if same domain: https://yourdomain.com/api

# ============================================
# Facebook Integration (Optional)
# ============================================
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_VERIFY_TOKEN=your_verify_token_here

# ============================================
# Chatwoot Integration (Optional)
# ============================================
CHATWOOT_WEBHOOK_URL=https://api.yourdomain.com/api/chatwoot/webhooks/chatwoot
LIVE_WEBHOOK_URL=https://api.yourdomain.com/api/webhooks/facebook
LOCAL_WEBHOOK_URL=https://api.yourdomain.com/api/webhooks/facebook

# ============================================
# File Upload Configuration
# ============================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# ============================================
# Additional Configuration (Optional)
# ============================================
# Email Configuration (if sending emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=noreply@yourdomain.com

# ============================================
# Security Notes
# ============================================
# 1. Never commit this file to version control
# 2. Use strong, random values for JWT_SECRET
# 3. Set file permissions to 600: chmod 600 .env
# 4. Keep database credentials secure
# 5. Use environment-specific values for production


