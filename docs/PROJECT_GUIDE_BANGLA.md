# Omni CRM - সম্পূর্ণ সিস্টেম গাইড (বাংলা)

এই ডকুমেন্টেশন Omni CRM সিস্টেমের সম্পূর্ণ কার্যপ্রক্রিয়া, ক্লায়েন্ট কীভাবে প্রজেক্ট শুরু করে এবং কিভাবে পুরো সিস্টেম পরিচালিত হয় তার বিস্তারিত বর্ণনা প্রদান করে।

---

## 📋 সুচিপত্র

1. [সিস্টেম পরিচিতি](#সিস্টেম-পরিচিতি)
2. [সিস্টেম আর্কিটেকচার](#সিস্টেম-আর্কিটেকচার)
3. [ক্লায়েন্ট রেজিস্ট্রেশন প্রক্রিয়া](#ক্লায়েন্ট-রেজিস্ট্রেশন-প্রক্রিয়া)
4. [ক্লায়েন্ট প্রজেক্ট শুরু করার প্রক্রিয়া](#ক্লায়েন্ট-প্রজেক্ট-শুরু-করার-প্রক্রিয়া)
5. [প্রশাসক/কোম্পানির দৃষ্টিকোণ থেকে সিস্টেম পরিচালনা](#প্রশাসককোম্পানির-দৃষ্টিকোণ-থেকে-সিস্টেম-পরিচালনা)
6. [পুরো সিস্টেমের কার্যপ্রবাহ](#পুরো-সিস্টেমের-কার্যপ্রবাহ)
7. [প্রধান মডিউলসমূহ](#প্রধান-মডিউলসমূহ)
8. [ডাটাবেস স্ট্রাকচার](#ডাটাবেস-স্ট্রাকচার)

---

## 🎯 সিস্টেম পরিচিতি

### Omni CRM কি?

**Omni CRM** হল একটি সম্পূর্ণ Enterprise Resource Planning (ERP) এবং Customer Relationship Management (CRM) সিস্টেম যা ব্যবসায়িক সকল কার্যক্রম পরিচালনার জন্য তৈরি করা হয়েছে।

### প্রধান বৈশিষ্ট্যসমূহ:

- ✅ **মাল্টি-টেন্যান্ট আর্কিটেকচার**: একাধিক কোম্পানি একই সিস্টেমে কাজ করতে পারে
- ✅ **ক্লায়েন্ট ম্যানেজমেন্ট**: ক্লায়েন্টদের জন্য আলাদা পোর্টাল
- ✅ **প্রজেক্ট ম্যানেজমেন্ট**: ক্লায়েন্টরা প্রজেক্ট তৈরি, ট্র্যাক এবং ইনভয়েস পেতে পারে
- ✅ **CRM (Customer Relationship Management)**: লিড, ক্লায়েন্ট এবং ক্যাম্পেইন ব্যবস্থাপনা
- ✅ **ইনবক্স ম্যানেজমেন্ট**: Facebook এবং Chatwoot এর মাধ্যমে সামাজিক মিডিয়া মেসেজিং
- ✅ **ফাইন্যান্স ম্যানেজমেন্ট**: ইনভয়েস, পেমেন্ট এবং আর্থিক লেনদেন
- ✅ **টাস্ক ম্যানেজমেন্ট**: এমপ্লয়িদের জন্য টাস্ক অ্যাসাইন এবং ট্র্যাকিং
- ✅ **রোল-বেসড এক্সেস কন্ট্রোল**: পারমিশন-ভিত্তিক ইউজার অ্যাক্সেস

---

## 🏗️ সিস্টেম আর্কিটেকচার

### প্রযুক্তি স্ট্যাক

#### Frontend (ক্লায়েন্ট সাইড)
- **React 19** with TypeScript
- **Vite** - Build tool এবং dev server
- **Tailwind CSS** - Styling
- **React Query** - Server state management
- **React Hook Form + Zod** - Form validation
- **React Router DOM v7** - Routing

#### Backend (সার্ভার সাইড)
- **Node.js** with Express
- **TypeScript** (strict mode)
- **Prisma ORM** - Database operations
- **MySQL** - Database
- **JWT** - Authentication
- **bcryptjs** - Password hashing

### সিস্টেম কাঠামো

```
Omni CRM
├── Client (Frontend)
│   ├── React Application
│   ├── Admin Dashboard (/admin/*)
│   ├── Client Portal (/client/*)
│   └── Public Pages (/)
│
├── Server (Backend)
│   ├── API Routes (/api/*)
│   ├── Controllers
│   ├── Services
│   ├── Middleware
│   └── Database (Prisma)
│
└── Database (MySQL)
    ├── Companies
    ├── Users (with Roles)
    ├── Clients
    ├── Projects
    ├── Services
    ├── Invoices
    └── ... (অন্যান্য টেবিল)
```

### মাল্টি-টেন্যান্ট আর্কিটেকচার

- প্রতিটি **Company** এর নিজস্ব ডেটা
- সকল ডেটা `companyId` দিয়ে ফিল্টার করা হয়
- একাধিক কোম্পানি একই সিস্টেম ব্যবহার করতে পারে
- প্রতিটি ইউজার একটি কোম্পানির সাথে সংযুক্ত

---

## 👤 ক্লায়েন্ট রেজিস্ট্রেশন প্রক্রিয়া

### ধাপ ১: রেজিস্ট্রেশন পেজে যাওয়া

1. ব্রাউজারে সিস্টেমের URL খুলুন (উদাহরণ: `https://yourdomain.com`)
2. হোমপেজ বা ল্যান্ডিং পেজে **"Register"** বা **"Sign Up"** বাটনে ক্লিক করুন
3. অথবা সরাসরি `/register` URL-এ যান

### ধাপ ২: ক্লায়েন্ট হিসাবে রেজিস্ট্রেশন

1. **Email** দিন
2. **Password** দিন (নূন্যতম ৬ অক্ষর)
3. **Confirm Password** দিন (password confirmation)
4. **Register** বাটনে ক্লিক করুন

### ধাপ ৩: ব্যাকগ্রাউন্ড প্রক্রিয়া

সিস্টেম স্বয়ংক্রিয়ভাবে:

1. **Default Company (ID = 1)** খুঁজে বের করে
2. **Client Role** তৈরি করে (যদি না থাকে)
3. নতুন **User** তৈরি করে:
   - Email (lowercase)
   - Hashed password (bcrypt)
   - Role: Client
   - Company: Default Company (ID = 1)
4. **Client Record** তৈরি করে `clients` টেবিলে:
   - Name: Email এর প্রথম অংশ (আগে @)
   - Contact Info: { email: user email }
   - Company ID: Default Company
5. **JWT Token** জেনারেট করে
6. Token localStorage এ সেভ করে

### ধাপ ৪: লগইন এবং রিডাইরেক্ট

1. রেজিস্ট্রেশন সফল হলে **JWT Token** পাওয়া যায়
2. Token `localStorage` এ সংরক্ষিত হয়
3. স্বয়ংক্রিয়ভাবে `/client/dashboard` এ রিডাইরেক্ট হয়

### ক্লায়েন্ট পারমিশন

ক্লায়েন্টদের নিম্নলিখিত পারমিশন থাকে:
- ✅ নিজের প্রজেক্ট দেখতে পারে
- ✅ নতুন প্রজেক্ট তৈরি করতে পারে
- ✅ ক্যাম্পেইন লিড দেখতে পারে
- ❌ এমপ্লয়িদের দেখতে পারে না
- ❌ ফাইন্যান্স ম্যানেজ করতে পারে না
- ❌ ইনবক্স ম্যানেজ করতে পারে না

---

## 📁 ক্লায়েন্ট প্রজেক্ট শুরু করার প্রক্রিয়া

### ধাপ ১: সার্ভিস নির্বাচন

1. ক্লায়েন্ট ড্যাশবোর্ডে `/client/projects` পেজে যান
2. **"New Project"** বাটনে ক্লিক করুন
3. একটি **Service** নির্বাচন করুন (যেগুলো কোম্পানি তৈরি করেছে)
   - Service Title
   - Service Details/Description
   - Base Price
   - Delivery Start Date
   - Delivery End Date

### ধাপ ২: প্রজেক্ট কনফিগারেশন

Service নির্বাচনের পর:

1. **Title**: প্রজেক্টের নাম (সার্ভিস টাইটেল থেকে অটো-ফিল)
2. **Description**: প্রজেক্টের বিস্তারিত (সার্ভিস ডিটেইল থেকে অটো-ফিল)
3. **Budget**: সার্ভিস প্রাইসের ৫০% থেকে ১৫০% এর মধ্যে (adjustable)
   - উদাহরণ: Service Price $1000 হলে, Budget $500 - $1500 হতে পারে
4. **Delivery Start Date**: প্রজেক্ট শুরু হওয়ার তারিখ
5. **Delivery End Date**: প্রজেক্ট শেষ হওয়ার তারিখ
6. **Timeframe**: প্রজেক্টের সময়কাল (যেমন: "4 weeks", "30 days")

### ধাপ ৩: প্রজেক্ট তৈরি

1. **"Create Project"** বাটনে ক্লিক করুন
2. প্রজেক্ট **Draft** status এ তৈরি হয়
3. প্রজেক্ট লিস্টে নতুন প্রজেক্ট দেখা যাবে

### ধাপ ৪: প্রজেক্ট সাইন করা (Signature)

1. Draft status এর প্রজেক্টে **"Sign"** বাটন দেখাবে
2. "Sign" বাটনে ক্লিক করুন
3. `/client/projects/{projectId}/sign` পেজে যাবেন
4. **E-Signature Pad** এ স্বাক্ষর করুন
5. **"Submit Signature"** বাটনে ক্লিক করুন

### ধাপ ৫: প্রজেক্ট সাবমিট হওয়ার পর

স্বাক্ষর জমা দেওয়ার পর:

1. প্রজেক্ট status **Draft** থেকে **Submitted** হয়ে যায়
2. কোম্পানি (Admin) প্রজেক্ট দেখতে পাবে
3. Admin প্রজেক্ট **StartedWorking** বা **InProgress** করতে পারে
4. কোম্পানি প্রজেক্টের জন্য **Invoice** তৈরি করতে পারে

### প্রজেক্ট স্ট্যাটাস ফ্লো

```
Draft → Submitted → StartedWorking → InProgress → Completed
                    ↓
                 Cancelled
```

- **Draft**: প্রজেক্ট তৈরি হয়েছে কিন্তু সাইন করা হয়নি
- **Submitted**: ক্লায়েন্ট সাইন করেছে, কোম্পানির অপেক্ষায়
- **StartedWorking**: কোম্পানি কাজ শুরু করেছে
- **InProgress**: কাজ চলছে
- **Completed**: কাজ সম্পন্ন হয়েছে
- **Cancelled**: প্রজেক্ট বাতিল

---

## 🏢 প্রশাসক/কোম্পানির দৃষ্টিকোণ থেকে সিস্টেম পরিচালনা

### সিস্টেম সেটআপ প্রক্রিয়া

#### ধাপ ১: কোম্পানি তৈরি

1. **SuperAdmin** হিসাবে লগইন করুন
2. `/admin/companies` পেজে যান
3. নতুন কোম্পানি তৈরি করুন:
   - Company Name
   - Email
   - Phone
   - Address
   - Industry
   - Website

#### ধাপ ২: সার্ভিস তৈরি

1. `/admin/services` পেজে যান
2. **"New Service"** বাটনে ক্লিক করুন
3. Service তথ্য দিন:
   - **Title**: সার্ভিসের নাম (যেমন: "Website Development")
   - **Details**: সার্ভিসের বিস্তারিত বর্ণনা
   - **Pricing**: সার্ভিসের মূল্য
   - **Delivery Start Date**: ন্যূনতম delivery start date
   - **Delivery End Date**: সর্বোচ্চ delivery end date
   - **Is Active**: সার্ভিস active আছে কিনা

4. **Save** করুন

#### ধাপ ৩: রোল এবং পারমিশন সেটআপ

1. `/admin/roles` পেজে যান
2. প্রয়োজনীয় Roles তৈরি করুন:
   - **SuperAdmin**: সব পারমিশন
   - **Admin**: কোম্পানি লেভেল admin
   - **Manager**: ডিপার্টমেন্ট ম্যানেজার
   - **Employee**: সাধারণ এমপ্লয়ি
   - **Client**: ক্লায়েন্ট (স্বয়ংক্রিয়ভাবে তৈরি হয়)

3. প্রতিটি Role-এ পারমিশন সেট করুন:
   ```json
   {
     "can_manage_users": true,
     "can_view_leads": true,
     "can_manage_inbox": false,
     "can_reply_social": false,
     ...
   }
   ```

#### ধাপ ৪: এমপ্লয়ি তৈরি

1. `/admin/employees` পেজে যান
2. **"Add Employee"** বাটনে ক্লিক করুন
3. এমপ্লয়ি তথ্য দিন:
   - **Name**: নাম
   - **Email**: ইমেইল
   - **Phone**: ফোন নম্বর
   - **Official Data**: অফিসিয়াল তথ্য (designation, department, salary, etc.)
   - **Role**: Role নির্বাচন করুন (Designation role থেকে নেওয়া হয়)

4. **Save** করুন
5. এমপ্লয়ির জন্য User account তৈরি হবে

### প্রজেক্ট ম্যানেজমেন্ট (কোম্পানির দৃষ্টিকোণ)

#### প্রজেক্ট দেখা

1. `/admin/projects-clients` পেজে যান
2. "Projects" ট্যাবে যান
3. সব প্রজেক্টের লিস্ট দেখতে পাবেন:
   - Project Title
   - Client Email
   - Company Name
   - Budget
   - Status

#### প্রজেক্ট স্ট্যাটাস আপডেট

1. প্রজেক্টে **"View"** বাটনে ক্লিক করুন
2. প্রজেক্ট ডিটেইল মোডাল খুলবে
3. Status পরিবর্তন করুন:
   - Submitted → StartedWorking
   - StartedWorking → InProgress
   - InProgress → Completed

#### ইনভয়েস তৈরি

1. প্রজেক্ট ডিটেইল মোডালে যান
2. **"Create Invoice"** বা `/admin/finance/invoices` পেজে যান
3. নতুন Invoice তৈরি করুন:
   - **Client**: ক্লায়েন্ট নির্বাচন করুন
   - **Project**: প্রজেক্ট নির্বাচন করুন
   - **Invoice Items**: 
     - Description
     - Quantity
     - Unit Price
   - **Issue Date**: Invoice তারিখ
   - **Due Date**: পেমেন্ট due date

4. Invoice PDF জেনারেট হবে
5. ক্লায়েন্ট তাদের portal-এ Invoice দেখতে পাবে

---

## 🔄 পুরো সিস্টেমের কার্যপ্রবাহ

### সম্পূর্ণ প্রজেক্ট লাইফসাইকেল

```
┌─────────────────────────────────────────────────────────────┐
│                    কোম্পানি সেটআপ                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Company তৈরি (SuperAdmin)          │
        │ 2. Services তৈরি (Admin)              │
        │ 3. Roles & Permissions সেটআপ          │
        │ 4. Employees তৈরি                     │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    ক্লায়েন্ট রেজিস্ট্রেশন                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. ক্লায়েন্ট Register করে             │
        │ 2. Client Role assign হয়              │
        │ 3. Client Record তৈরি হয়              │
        │ 4. Dashboard-এ redirect হয়            │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    প্রজেক্ট তৈরি                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Service নির্বাচন                    │
        │ 2. Budget & Dates কনফিগার             │
        │ 3. Project Draft হিসেবে তৈরি          │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    প্রজেক্ট সাইন                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. E-Signature Pad-এ সাইন            │
        │ 2. Signature submit                   │
        │ 3. Status: Draft → Submitted          │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    কোম্পানি রিভিউ                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Admin প্রজেক্ট Review করে          │
        │ 2. Status: Submitted → StartedWorking │
        │ 3. কাজ শুরু                             │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    কাজের অগ্রগতি                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Status: StartedWorking → InProgress│
        │ 2. Tasks assign করা হয় এমপ্লয়িদের   │
        │ 3. কাজ চলতে থাকে                      │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    ইনভয়েস তৈরি                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Invoice তৈরি (Admin)               │
        │ 2. Invoice Items add করা              │
        │ 3. PDF generate                        │
        │ 4. ক্লায়েন্ট-এ send                  │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    পেমেন্ট                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. ক্লায়েন্ট Invoice দেখে             │
        │ 2. Payment Gateway দিয়ে Payment করে  │
        │ 3. Invoice status: Unpaid → Paid      │
        └───────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    প্রজেক্ট সম্পন্ন                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │ 1. Status: InProgress → Completed     │
        │ 2. Final deliverables হস্তান্তর       │
        │ 3. প্রজেক্ট বন্ধ                         │
        └───────────────────────────────────────┘
```

---

## 📦 প্রধান মডিউলসমূহ

### ১. Authentication & Authorization

- **User Registration**: Email এবং Password দিয়ে রেজিস্ট্রেশন
- **Login**: JWT Token-based authentication
- **Roles**: SuperAdmin, Admin, Manager, Employee, Client
- **Permissions**: Role-based access control (RBAC)
- **Multi-tenant**: Company-based data isolation

### ২. Company Management

- **Company CRUD**: কোম্পানি তৈরি, দেখানো, আপডেট, ডিলিট
- **Multi-tenant Support**: একাধিক কোম্পানি একই সিস্টেমে
- **Company Settings**: কোম্পানি তথ্য ম্যানেজমেন্ট

### ৩. User & Employee Management

- **User Management**: ইউজার তৈরি, আপডেট, ডিলিট
- **Employee Management**: এমপ্লয়িদের অফিসিয়াল তথ্য
- **Departments**: ডিপার্টমেন্ট তৈরি এবং ম্যানেজমেন্ট
- **Employee Groups**: এমপ্লয়িদের গ্রুপ তৈরি

### ৪. Service Management

- **Service CRUD**: সার্ভিস তৈরি, আপডেট, ডিলিট
- **Service Categories**: সার্ভিস ক্যাটাগরি
- **Pricing**: সার্ভিস প্রাইসিং
- **Delivery Dates**: Delivery timeline

### ৫. Project Management

- **Project Creation**: ক্লায়েন্টরা প্রজেক্ট তৈরি করতে পারে
- **Project Status**: Draft, Submitted, StartedWorking, InProgress, Completed, Cancelled
- **E-Signature**: Digital signature support
- **Project Tracking**: প্রজেক্টের অগ্রগতি ট্র্যাক করা

### ৬. Client Management

- **Client Portal**: ক্লায়েন্টদের জন্য আলাদা ড্যাশবোর্ড
- **Client Projects**: ক্লায়েন্টের প্রজেক্টসমূহ
- **Client Invoices**: ক্লায়েন্টের ইনভয়েসসমূহ
- **Client Campaigns**: ক্লায়েন্টের ক্যাম্পেইনসমূহ

### ৭. Finance Management

- **Invoices**: Invoice তৈরি, PDF generate, status management
- **Payments**: Payment gateway integration
- **Transactions**: Income/Expense tracking
- **Accounts Receivable/Payable**: Receivables এবং Payables
- **Budgets**: Budget planning এবং tracking

### ৮. CRM (Customer Relationship Management)

- **Leads**: Lead তৈরি, assign, status update
- **Lead Categories**: Lead categorization
- **Lead Interests**: Lead interests tracking
- **Campaigns**: Marketing campaigns
- **Campaign Clients**: Campaign-এ client assignment

### ৯. Task Management

- **Tasks**: Task তৈরি, assign, status update
- **Task Priorities**: Low, Medium, High
- **Task Status**: Todo, InProgress, Done
- **Task Comments**: Task-এ comment করা

### ১০. Inbox Management

- **Social Conversations**: Facebook এবং Chatwoot conversations
- **Social Messages**: Message threads
- **Reply Functionality**: Direct reply from dashboard
- **Integration Management**: Facebook/Chatwoot API integration

### ১১. Product Management

- **Products**: Product catalog
- **Product Categories**: Product categorization
- **Product Images**: Product image upload
- **Quick Replies**: Product-based quick replies

### ১২. Integration Management

- **Facebook Integration**: Facebook Page messaging
- **Chatwoot Integration**: Chatwoot CRM integration
- **Webhook Management**: Webhook setup এবং management

---

## 🗄️ ডাটাবেস স্ট্রাকচার

### প্রধান টেবিলসমূহ

#### ১. Companies
```sql
- id (Primary Key)
- name
- email
- phone
- address
- industry
- website
- is_active
- created_at
- updated_at
```

#### ২. Users
```sql
- id (UUID, Primary Key)
- name
- email
- phone
- password_hash
- role_id (Foreign Key → Roles)
- company_id (Foreign Key → Companies)
- profile_image
- e_signature
- created_at
- updated_at
```

#### ৩. Roles
```sql
- id (Primary Key)
- name (Unique: SuperAdmin, Admin, Manager, Employee, Client)
- permissions (JSON)
- created_at
- updated_at
```

#### ৪. Employees
```sql
- id (Primary Key)
- user_id (Foreign Key → Users)
- department_id (Foreign Key → Departments)
- designation
- salary
- join_date
- official_data (JSON)
- created_at
- updated_at
```

#### ৫. Services
```sql
- id (Primary Key)
- company_id (Foreign Key → Companies)
- title
- details
- pricing
- delivery_start_date
- delivery_end_date
- is_active
- created_at
- updated_at
```

#### ৬. Projects
```sql
- id (Primary Key)
- company_id (Foreign Key → Companies)
- client_id (Foreign Key → Users)
- service_id (Foreign Key → Services)
- title
- description
- budget
- delivery_start_date
- delivery_end_date
- time
- status (Draft, Submitted, StartedWorking, InProgress, Completed, Cancelled)
- signature (E-signature)
- document_url
- signed_at
- created_at
- updated_at
```

#### ৭. Clients
```sql
- id (Primary Key)
- company_id (Foreign Key → Companies)
- name
- contact_info (JSON: {email, phone, ...})
- address
- created_at
- updated_at
```

#### ৮. Invoices
```sql
- id (Primary Key)
- company_id (Foreign Key → Companies)
- client_id (Foreign Key → Clients)
- project_id (Foreign Key → Projects)
- invoice_number
- issue_date
- due_date
- total_amount
- status (Draft, Sent, Paid, Overdue, Cancelled)
- created_at
- updated_at
```

#### ৯. Leads
```sql
- id (Primary Key)
- created_by (Foreign Key → Users)
- assigned_to (Foreign Key → Employees)
- title
- description
- source (Website, Referral, SocialMedia, Email, Phone, Inbox, Other)
- status (New, Contacted, Qualified, Negotiation, Won, Lost)
- value
- customer_name
- phone
- category_id (Foreign Key → LeadCategories)
- interest_id (Foreign Key → LeadInterests)
- campaign_id (Foreign Key → Campaigns)
- conversation_id (Foreign Key → SocialConversations)
- created_at
- updated_at
```

#### ১০. Tasks
```sql
- id (Primary Key)
- company_id (Foreign Key → Companies)
- assigned_to (Foreign Key → Employees)
- title
- description
- priority (Low, Medium, High)
- due_date
- status (Todo, InProgress, Done)
- created_at
- updated_at
```

### রিলেশনশিপ

1. **Company → Users**: One-to-Many (একটি কোম্পানিতে অনেক User)
2. **User → Employee**: One-to-One (একজন User-এর একটি Employee profile)
3. **User → Projects**: One-to-Many (একজন Client-এর অনেক প্রজেক্ট)
4. **Company → Services**: One-to-Many (একটি কোম্পানিতে অনেক Service)
5. **Service → Projects**: One-to-Many (একটি Service থেকে অনেক প্রজেক্ট)
6. **Project → Invoices**: One-to-Many (একটি প্রজেক্টের অনেক Invoice)
7. **Client → Projects**: One-to-Many (একজন Client-এর অনেক প্রজেক্ট)
8. **Employee → Tasks**: One-to-Many (একজন Employee-এর অনেক Task)
9. **Employee → Leads**: One-to-Many (একজন Employee-এর অনেক Lead)

---

## 🔐 সিকিউরিটি এবং পারমিশন

### Authentication

- **JWT Token**: সকল API request-এ JWT token প্রয়োজন
- **Password Hashing**: bcryptjs দিয়ে password hash করা হয়
- **Token Storage**: Frontend-এ localStorage-এ token সংরক্ষণ

### Authorization

- **Role-Based Access Control (RBAC)**: Role-ভিত্তিক access control
- **Permission-Based**: Permission-ভিত্তিক feature access
- **SuperAdmin Bypass**: SuperAdmin সব permission bypass করতে পারে

### পারমিশন ফরম্যাট

```json
{
  "can_manage_users": true/false,
  "can_view_leads": true/false,
  "can_manage_inbox": true/false,
  "can_reply_social": true/false,
  "can_manage_finance": true/false,
  "can_create_projects": true/false,
  "can_view_own_projects": true/false,
  ...
}
```

---

## 📱 API Endpoints

### Authentication

- `POST /api/auth/register` - User registration
- `POST /api/auth/register-client` - Client registration
- `POST /api/auth/login` - User login
- `GET /api/auth/me` - Get current user profile

### Projects

- `GET /api/projects` - Get all projects (client's own)
- `POST /api/projects` - Create new project
- `GET /api/projects/:id` - Get project details
- `PUT /api/projects/:id` - Update project
- `POST /api/projects/:id/sign` - Sign project

### Services

- `GET /api/services` - Get all services
- `POST /api/services` - Create service (Admin)
- `PUT /api/services/:id` - Update service (Admin)
- `DELETE /api/services/:id` - Delete service (Admin)

### Invoices

- `GET /api/invoices` - Get all invoices
- `POST /api/invoices` - Create invoice (Admin)
- `GET /api/invoices/:id` - Get invoice details
- `GET /api/invoices/:id/pdf` - Download invoice PDF

### Admin

- `GET /api/admin/projects` - Get all projects (Admin)
- `GET /api/admin/clients` - Get all clients (Admin)
- `PUT /api/admin/projects/:id/status` - Update project status (Admin)

---

## 🚀 Deployment Process

### Development Setup

1. **Database Setup**:
   ```bash
   cd server
   npx prisma migrate dev
   npx prisma generate
   npm run seed  # Optional: Seed data
   ```

2. **Environment Variables**:
   - `server/.env`: Database URL, JWT Secret, etc.
   - `client/.env`: API URL

3. **Run Development Servers**:
   ```bash
   # Terminal 1: Backend
   cd server
   npm run dev  # Runs on http://localhost:5000
   
   # Terminal 2: Frontend
   cd client
   npm run dev  # Runs on http://localhost:5173
   ```

### Production Deployment

1. **Build**:
   ```bash
   # Backend
   cd server
   npm run build
   
   # Frontend
   cd client
   npm run build
   ```

2. **Database Migration**:
   ```bash
   cd server
   npx prisma migrate deploy
   ```

3. **PM2** (Process Manager):
   ```bash
   pm2 start server/dist/server.js --name omni-api
   ```

4. **Nginx** (Reverse Proxy):
   - Frontend: Serve static files
   - Backend: Proxy API requests to Node.js

---

## 📝 সচরাচর জিজ্ঞাসা (FAQ)

### Q: ক্লায়েন্ট কিভাবে প্রজেক্ট শুরু করে?

**A:** 
1. ক্লায়েন্ট রেজিস্ট্রেশন করে
2. Client Dashboard-এ যায় (`/client/dashboard`)
3. "Projects" মেনুতে যায় (`/client/projects`)
4. "New Project" বাটনে ক্লিক করে
5. একটি Service নির্বাচন করে
6. Budget এবং Delivery Dates কনফিগার করে
7. প্রজেক্ট তৈরি করে
8. E-Signature দিয়ে প্রজেক্ট সাইন করে

### Q: কোম্পানি কিভাবে সার্ভিস তৈরি করে?

**A:**
1. Admin হিসাবে লগইন করে
2. `/admin/services` পেজে যায়
3. "New Service" বাটনে ক্লিক করে
4. Service Title, Details, Pricing, Delivery Dates দেয়
5. Save করে

### Q: ইনভয়েস কিভাবে তৈরি হয়?

**A:**
1. Admin প্রজেক্ট-এর জন্য Invoice তৈরি করে
2. Invoice Items (Description, Quantity, Unit Price) যোগ করে
3. Invoice PDF generate হয়
4. ক্লায়েন্ট তাদের Portal-এ Invoice দেখতে পায়
5. Payment Gateway দিয়ে Payment করতে পারে

### Q: মাল্টি-টেন্যান্ট কিভাবে কাজ করে?

**A:**
- প্রতিটি Company-এর নিজস্ব Data
- সকল Database Query-তে `companyId` ফিল্টার করা হয়
- User registration-এ Company ID প্রয়োজন
- Client registration-এ default Company (ID = 1) ব্যবহার করা হয়

---

## 📞 Support এবং Documentation

- **Database Schema**: `docs/database_schema.md`
- **Deployment Guide**: `docs/DEPLOYMENT_GUIDE_BANGLA.md`
- **API Documentation**: API endpoints-এর জন্য code comments দেখুন

---

**আপডেট তারিখ**: ২০২৪
**ভার্সন**: 1.0.0

