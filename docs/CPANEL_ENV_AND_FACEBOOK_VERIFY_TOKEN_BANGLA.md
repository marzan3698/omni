# cPanel এ Environment Variables ও Facebook Verify Token – সম্পূর্ণ গাইড

## 📋 বিষয়বস্তু

1. [cPanel এ env কিভাবে কাজ করে](#cpanel-এ-env-কিভাবে-কাজ-করে)
2. [Verify Token কী এবং কিভাবে পাবেন](#verify-token-কী-এবং-কিভাবে-পাবেন)
3. [এখন কি করবেন (স্টেপ বাই স্টেপ)](#এখন-কি-করবেন-স্টেপ-বাই-স্টেপ)
4. [টোটাল ডিপ্লয়মেন্ট সারাংশ](#টোটাল-ডিপ্লয়মেন্ট-সারাংশ)

---

## cPanel এ env কিভাবে কাজ করে

Omni CRM ব্যাকেন্ড **দুইভাবে** environment variables পড়ে:

### পদ্ধতি ১: `.env` ফাইল (Application Root এ)

- **কোথায়:** cPanel এ আপনার Node.js অ্যাপের **Application Root** (যেমন `~/api` বা `/home/paaera/api`) এর ভেতরে `.env` ফাইল।
- **কিভাবে তৈরি হয়:**
  - GitHub থেকে অটো ডিপ্লয় করলে সাধারণত `.env` **ডিপ্লয় হয় না** (.gitignore এ থাকার জন্য)। তাই আপনি হয় **ম্যানুয়ালি** ফাইল তৈরি করবেন, নয়তো **পদ্ধতি ২** ব্যবহার করবেন।
  - অথবা Super Admin ড্যাশবোর্ড থেকে **Facebook Webhook Configuration** সেভ করলে অ্যাপ যদি সেই ফোল্ডারে লিখতে পারে তাহলে `.env` তৈরি/আপডেট হতে পারে।
- **কি রাখবেন:** নিচের [প্রয়োজনীয় ভেরিয়েবল](#প্রয়োজনীয়-environment-variables) অনুযায়ী।

### পদ্ধতি ২: cPanel Node.js Environment Variables

- **কোথায়:** cPanel → **Node.js Selector** → আপনার অ্যাপ নির্বাচন → **Edit (পেন্সিল আইকন)** → নিচে **Environment variables** সেকশন।
- **কিভাবে:** প্রতিটি ভেরিয়েবলের জন্য **Name** ও **Value** দিয়ে **ADD VARIABLE** ক্লিক করুন। সব দেয়া হলে **Save** করুন। তারপর অ্যাপ **Stop** করে আবার **Start** করুন যাতে নতুন env লোড হয়।
- **কেন ভালো:** cPanel নিজে process.env এ সাপ্লাই করে, তাই `.env` ফাইল না থাকলেও কাজ করে। GitHub ডিপ্লয়মেন্টে `.env` না থাকলেও সমস্যা নেই।

### অ্যাপ কোনটা ব্যবহার করে?

- অ্যাপ প্রথমে **Application Root** এ `.env` খুঁজে। **পেলে** সেখান থেকে Facebook config (App ID, Secret, **Verify Token**, OAuth Redirect URI) পড়ে।
- **না পেলে** (অথবা .env এ কোনো key না থাকলে) **process.env** ব্যবহার করে (অর্থাৎ cPanel এ দেওয়া Environment Variables)।

তাই **যেকোনো এক জায়গায়** (হয় `.env` ফাইল, নয় cPanel Environment Variables) সঠিক মান দিলেই হবে।

---

## Verify Token কী এবং কিভাবে পাবেন

### গুরুত্বপূর্ণ: Verify Token “কোথাও থেকে পাওয়া” যায় না

- **Verify Token** এমন একটা **গোপন টেক্সট** যেটা **আপনিই বেছে নেন** এবং **দুই জায়গায় একই মান** দেন:
  1. **আপনার সার্ভার** (cPanel env বা `.env`) → `FACEBOOK_VERIFY_TOKEN=আপনার_টেক্সট`
  2. **Facebook App** → Messenger API Settings → Configure webhooks → **Verify token** ফিল্ডে একই টেক্সট

Facebook যখন ওয়েবহুক ভেরিফাই করতে আপনার URL এ request পাঠায়, তখন এই টেক্সট মিললে ভেরিফিকেশন সফল হয়।

### “পেতে” কি করতে হবে (অর্থাৎ সেট করতে হবে)

1. **একটা টেক্সট বেছে নিন**  
   উদাহরণ: `omni_crm_webhook_2024_secure` বা `paaera_fb_verify_xyz` — যেকোনো শক্ত, অনুমান করা কঠিন এমন টেক্সট।

2. **সেই টেক্সট সার্ভার env এ দিন**  
   - হয় **cPanel → Node.js App → Environment variables** এ:  
     `FACEBOOK_VERIFY_TOKEN` = `আপনার_টেক্সট`  
   - অথবা **`.env`** ফাইলে (যদি ব্যবহার করেন):  
     `FACEBOOK_VERIFY_TOKEN=আপনার_টেক্সট`

3. **অ্যাপ রিস্টার্ট করুন**  
   cPanel এ অ্যাপ Stop করে আবার Start করুন (env আপডেট হলে)।

4. **এখন ড্যাশবোর্ডে দেখাবে**  
   Admin → **Environment File Editing** → **প্রয়োজনীয় URL** কার্ডে **“2. Verify token”** এ সেই একই টেক্সট দেখাবে। সেখান থেকে **কপি** করুন।

5. **Facebook এ দিন**  
   Facebook Developer Console → আপনার অ্যাপ → Use cases → **Messenger** → **Configure webhooks** → **Verify token** ফিল্ডে কপি করা টেক্সট পেস্ট করুন → **Verify and save**।

সংক্ষেপে: Verify Token **আপনি তৈরি/সেট করেন** — প্রথমে env এ, তারপর ড্যাশবোর্ড থেকে কপি করে Facebook এ দেন।

---

## এখন কি করবেন (স্টেপ বাই স্টেপ)

### ধাপ ১: Verify Token সেট করুন (cPanel)

**Option A – cPanel Environment Variables (সাধারণত সহজ)**

1. cPanel লগইন করুন।
2. **Node.js Selector** এ যান → আপনার অ্যাপ (যে অ্যাপে api.paaera.com / backend চালু) এ **Edit** ক্লিক করুন।
3. **Environment variables** সেকশনে যান।
4. নতুন ভেরিয়েবল যোগ করুন:
   - **Name:** `FACEBOOK_VERIFY_TOKEN`
   - **Value:** আপনার বেছে নেওয়া টেক্সট (যেমন: `omni_crm_webhook_2024_secure`)
5. যদি Facebook App ID, Secret, OAuth URL ও দিতে চান (যাতে Required URLs ও Facebook config ঠিক থাকে):
   - `FACEBOOK_APP_ID` = আপনার অ্যাপের App ID
   - `FACEBOOK_APP_SECRET` = App Secret (Facebook Dashboard → App settings → Basic)
   - `FACEBOOK_OAUTH_REDIRECT_URI` = `https://api.paaera.com/api/integrations/facebook/callback`
   - `API_URL` = `https://api.paaera.com` (প্রয়োজনীয় URL কার্ডের Base URL এর জন্য)
6. **Save** করুন। অ্যাপ **Stop** করে আবার **Start** করুন।

**Option B – Super Admin ড্যাশবোর্ড থেকে (যদি .env লিখতে পারে)**

1. `paaera.com` (অথবা আপনার ডোমেইন) এ লগইন করুন → **Environment File Editing** পেজে যান।
2. **Facebook Webhook Configuration** কার্ডে:
   - **Facebook Verify Token** ফিল্ডে আপনার বেছে নেওয়া টেক্সট দিন (উদাহরণ: `omni_crm_webhook_2024_secure`)।
   - বাকি ফিল্ড (App ID, App Secret, OAuth Redirect URI) প্রয়োজনমতো পূরণ করুন। OAuth Redirect URI: `https://api.paaera.com/api/integrations/facebook/callback`।
3. **Save Configuration** ক্লিক করুন।
4. যদি সার্ভার ওই ফোল্ডারে `.env` লিখতে পারে, তাহলে Verify Token সেভ হয়ে যাবে এবং **প্রয়োজনীয় URL** কার্ডে দেখাবে।

### ধাপ ২: প্রয়োজনীয় URL ও Verify Token কপি করুন

1. একই পেজে **“প্রয়োজনীয় URL (Required URLs for Facebook)”** কার্ডে যান।
2. এখন **Verify token** ফিল্ডে মান দেখা যাওয়া উচিত (যেটা আপনি ধাপ ১ এ সেট করেছেন)।
3. প্রতিটি মানের পাশের **কপি** বাটন দিয়ে কপি করুন:
   - **Callback URL** → Facebook Webhook এ দেবেন
   - **Verify token** → Facebook Verify token ফিল্ডে দেবেন
   - **OAuth Redirect URI** → প্রয়োজনে Facebook OAuth সেটিংসে

### ধাপ ৩: Facebook App এ কনফিগার করুন

1. [developers.facebook.com](https://developers.facebook.com) → আপনার অ্যাপ (যে অ্যাপ Messenger ব্যবহার করবে)।
2. **Use cases** → **Engage with customers on Messenger from Meta** → **Customize**।
3. **Messenger API Settings** → **Configure webhooks**:
   - **Callback URL:** ড্যাশবোর্ড থেকে কপি করা Callback URL (যেমন `https://api.paaera.com/api/webhooks/facebook`)।
   - **Verify token:** ড্যাশবোর্ড থেকে কপি করা Verify token।
4. **Verify and save** ক্লিক করুন।
5. এরপর **Generate access tokens** থেকে Facebook পেজ কানেক্ট করুন।

---

## প্রয়োজনীয় Environment Variables (সারাংশ)

| Variable | বর্ণনা | উদাহরণ (production) |
|----------|--------|----------------------|
| `NODE_ENV` | পরিবেশ | `production` |
| `PORT` | ব্যাকেন্ড পোর্ট | `5001` (বা cPanel দেওয়া পোর্ট) |
| `DATABASE_URL` | MySQL সংযোগ | `mysql://user:pass@localhost:3306/dbname` |
| `JWT_SECRET` | JWT সিক্রেট | দীর্ঘ র্যান্ডম স্ট্রিং |
| `CLIENT_URL` | ফ্রন্টেন্ড URL | `https://paaera.com` |
| `API_URL` বা `PUBLIC_URL` | ব্যাকেন্ড পাবলিক URL | `https://api.paaera.com` |
| `FACEBOOK_APP_ID` | Facebook App ID | সংখ্যা (Facebook Dashboard থেকে) |
| `FACEBOOK_APP_SECRET` | Facebook App Secret | Facebook Dashboard → Basic থেকে |
| **`FACEBOOK_VERIFY_TOKEN`** | **ওয়েবহুক ভেরিফাই টোকেন** | **আপনি বেছে নেওয়া গোপন টেক্সট** |
| `FACEBOOK_OAUTH_REDIRECT_URI` | OAuth callback | `https://api.paaera.com/api/integrations/facebook/callback` |

`API_URL` / `PUBLIC_URL` সেট থাকলে ড্যাশবোর্ডের **প্রয়োজনীয় URL** কার্ড সঠিক ডোমেইন দিয়ে URL দেখাবে।

---

## টোটাল ডিপ্লয়মেন্ট সারাংশ

### GitHub → cPanel অটো ডিপ্লয়

- রিপো **GitHub** এ পুশ করলে **GitHub Actions** চালু হয়।
- ওয়ার্কফ্লো **SSH** দিয়ে cPanel সার্ভারে কানেক্ট হয়ে কোড পুল/বিল্ড করে এবং ফাইলগুলো **Application Root** (যেমন `~/api`) এ রাখে।
- বিস্তারিত: [docs/GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md](./GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md)

### cPanel এ কী কী সেটআপ

1. **Node.js অ্যাপ**  
   - Node.js Selector দিয়ে অ্যাপ তৈরি, Application Root = `~/api` (বা আপনার ব্যাকেন্ড ফোল্ডার), Startup file = `dist/server.js` (বা প্রকল্প অনুযায়ী)।  
   - বিস্তারিত: [docs/CPANEL_NODEJS_SETUP.md](./CPANEL_NODEJS_SETUP.md), [docs/GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md](./GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md)

2. **Environment variables**  
   - হয় **cPanel Node.js App → Environment variables** এ সব প্রয়োজনীয় ভেরিয়েবল (উপরের টেবিল অনুযায়ী) যোগ করুন।  
   - অথবা **Application Root** এ `.env` ফাইল রেখে একই মান দিন (ডিপ্লয় স্ক্রিপ্ট যদি `.env` টাচ না করে তাহলে ম্যানুয়ালি বা অন্য উপায়ে তৈরি করতে হবে)।  
   - বিস্তারিত: একই গাইডের “Environment Variables Setup” এবং [docs/server.env.example](./server.env.example)।

3. **ডাটাবেস**  
   - cPanel MySQL দিয়ে ডাটাবেস ও ইউজার তৈরি, `DATABASE_URL` এ একই ক্রেডেনশিয়াল দিন।  
   - বিস্তারিত: [docs/GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md](./GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md)।

4. **Facebook (ওয়েবহুক + Verify Token)**  
   - উপরের ধাপ অনুযায়ী `FACEBOOK_VERIFY_TOKEN` (ও প্রয়োজন হলে বাকি Facebook env) cPanel env বা .env এ সেট করুন।  
   - ড্যাশবোর্ডের **প্রয়োজনীয় URL** থেকে Callback URL ও Verify token কপি করে Facebook App এ দিন।  
   - বিস্তারিত: [docs/facebook_integration_guide_bangla.md](./facebook_integration_guide_bangla.md) (যদি থাকে)।

### ডকুমেন্ট রেফারেন্স

| বিষয় | ফাইল |
|--------|------|
| GitHub → cPanel অটো ডিপ্লয় (বাংলা) | [GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md](./GITHUB_CPANEL_AUTO_DEPLOY_BANGLA.md) |
| cPanel Node.js সেটআপ | [CPANEL_NODEJS_SETUP.md](./CPANEL_NODEJS_SETUP.md) |
| সার্ভার env উদাহরণ | [server.env.example](./server.env.example) |
| সম্পূর্ণ ডিপ্লয় সেটআপ | [COMPLETE_DEPLOYMENT_SETUP.md](./COMPLETE_DEPLOYMENT_SETUP.md) |
| Facebook Verify Token + env (বাংলা) | এই ফাইল (CPANEL_ENV_AND_FACEBOOK_VERIFY_TOKEN_BANGLA.md) |

---

## সংক্ষেপে

- **cPanel এ env:** হয় `.env` ফাইল (Application Root), নয়তো **Node.js Selector → Environment variables**। যেকোনো একটায় সঠিক মান থাকলেই অ্যাপ সেটা ব্যবহার করবে।
- **Verify Token:** এটা আপনি নিজে বেছে নিয়ে **প্রথমে সার্ভার env এ** (`FACEBOOK_VERIFY_TOKEN`) সেট করবেন, তারপর **ড্যাশবোর্ডের প্রয়োজনীয় URL** থেকে কপি করে **Facebook App এর Verify token** ফিল্ডে দেবেন। এভাবে সেট করলেই আপনি “Verify token পেতে” পারবেন এবং ড্যাশবোর্ডে আর খালি থাকবে না।
