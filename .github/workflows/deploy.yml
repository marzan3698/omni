name: Deploy Omni CRM to cPanel

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy to cPanel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Frontend
        working-directory: ./client
        env:
          VITE_API_URL: https://imoics.com/api
        run: |
          npm ci
          npm run build
          echo "✅ Frontend build completed"

      - name: Build Backend
        working-directory: ./server
        env:
          PUPPETEER_SKIP_DOWNLOAD: true
        run: |
          npm ci --ignore-scripts
          npx prisma generate
          npm run build
          echo "✅ Backend build completed"

      - name: Deploy Frontend to public_html via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.CPANEL_HOST }}
          username: ${{ secrets.CPANEL_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "client/dist/*"
          target: "~/public_html/"
          strip_components: 2

      - name: Deploy Backend dist/ to server via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.CPANEL_HOST }}
          username: ${{ secrets.CPANEL_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "server/dist/*"
          target: "~/omni-repo/server/dist/"
          strip_components: 2

      - name: Setup .htaccess & Restart Backend
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.CPANEL_HOST }}
          username: ${{ secrets.CPANEL_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            echo "🚀 Post-deploy setup..."

            # Create .htaccess for React Router (exclude /api)
            cat > ~/public_html/.htaccess << 'EOF'
            <IfModule mod_rewrite.c>
              RewriteEngine On
              RewriteBase /
              RewriteRule ^index\.html$ - [L]
              RewriteCond %{REQUEST_FILENAME} !-f
              RewriteCond %{REQUEST_FILENAME} !-d
              RewriteRule . /index.html [L]
            </IfModule>
            EOF

            # Update backend source code
            cd ~/omni-repo
            git pull origin main
            echo "✅ Backend source & dist updated"

            # Restart Node.js app via Passenger touch
            mkdir -p ~/omni-repo/server/tmp
            touch ~/omni-repo/server/tmp/restart.txt
            echo "✅ Backend restart triggered"

            echo "🎉 Deployment complete!"

      - name: Deployment Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment completed successfully!"
          else
            echo "❌ Deployment failed! Check the logs above."
          fi
